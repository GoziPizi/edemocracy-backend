import express from 'express';

const LoginRouter = express.Router();

LoginRouter.post('/', async (req, res) => {
    //TODO
    try {
        //TODO
        res.status(200)
    } catch (error) {
        console.log(error);
    }
});